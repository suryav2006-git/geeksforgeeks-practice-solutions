#include <iostream>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    // code here
    int p = a + b;
    int q = a - b;
    int r = a * b;
    int s = a / b;
    int t = a % b;

    // The below code prints the output
    cout << p << " " << q << " " << r << " " << s << " " << t << endl;
    return 0;
}